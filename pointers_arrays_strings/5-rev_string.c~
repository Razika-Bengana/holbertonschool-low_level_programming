#include "main.h"
/**
 * rev_string - reverses a string
 * @s: input for the reverse
 * Return: void
 */
void rev_string(char *s)
{
	char rev = s[0];
	int i;

	for (i = 0; s[i] != '\0'; i++)
		;
	for (i = i; i--;)
	{
		rev = s[i];
}
