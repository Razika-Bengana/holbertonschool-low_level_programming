#include <stdio.h>
#include "main.h"
/**
 * add - function that adds two integers and returns the result
 *@m: an integer
 *@n: an integer
 *Return: a + b result
 */

int add(int m, int n)
{
	return (m + n);
}
