#include <stdio.h>
#include "main.h"
/**
 * int add(int, int) - function that adds two integers and returns the result
 *@m: an integer; @n: an integer
 */

int add(int m, int n)
{
	return (m + n);
}
